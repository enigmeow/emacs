(global-font-lock-mode t)
(setq transient-mark-mode t)
(setq compilation-skip-threshold 1)

(defconst myemacsdir (concat (getenv "STORMBIRDS") "/other/emacs/jsiegel"))

(setq load-path 
      (append (list 
        (concat (getenv "STORMBIRDS") "/other/emacs/jsiegel") 
        (concat (getenv "STORMBIRDS") "/other/emacs/asiegel")) 
              load-path))

(load "josh-emacs")

(put 'erase-buffer 'disabled nil)
(setq x-select-enable-clipboard t)
(setq require-final-newline 't)
